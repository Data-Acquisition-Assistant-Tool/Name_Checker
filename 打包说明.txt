Namechecker v1.2 打包说明
================================

📋 关于这个工具：
Namechecker 是一个文件名检查工具，用于比较Excel文件中的文件名与文件夹中的实际文件，检查文件完整性。

🔧 打包工具说明：
1. 快速打包Namechecker.bat - 一键打包脚本，推荐使用
2. build_exe.bat - 基础打包脚本  
3. build_detailed.py - 详细打包脚本（显示更多信息）

🚀 如何打包：
方式1（推荐）：双击运行 "快速打包Namechecker.bat"
方式2：在命令行中运行以下命令：
   python -m PyInstaller --onefile --windowed --name Namechecker_1.2 --hidden-import pandas --hidden-import numpy --hidden-import openpyxl --hidden-import pandas.io.excel --hidden-import pandas.io.formats.excel --hidden-import pandas.io.parsers --hidden-import et_xmlfile --hidden-import pytz --hidden-import dateutil --clean NameCheck_original.py

📦 打包要求：
- 已安装Python 3.x
- 已安装PyInstaller: pip install pyinstaller
- 已安装pandas: pip install pandas  
- 已安装openpyxl: pip install openpyxl

📁 生成的文件：
打包完成后，在 dist/ 目录下会生成 Namechecker_1.2.exe
文件大小约 36 MB（包含完整pandas依赖）

✅ 使用说明：
1. 将 Namechecker_1.2.exe 复制到任何Windows电脑
2. 双击运行，无需安装Python或其他依赖
3. 程序提供图形界面，操作简单直观

🔄 重新打包：
每次修改 NameCheck_original.py 后，只需重新运行打包脚本即可。
建议每次打包前更新版本号。

📝 版本历史：
v1.2 - 当前版本，包含完整的Excel文件处理功能

💡 技巧：
- 打包时使用 --clean 参数确保清理缓存
- 使用 --windowed 参数隐藏控制台窗口  
- 添加 --hidden-import 确保所有依赖都被包含
